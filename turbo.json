{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "globalDependencies": ["**/.env"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**",
        "storybook-static/**",
        ".next/**",
        "!.next/cache/**"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "outputs": ["coverage/**"]
    },
    "e2e": {
      "outputs": ["playwright-report/**", "test-results/**"]
    },
    "typecheck": {},
    "lint": {
      "dependsOn": ["^lint"]
    },
    "clean": {
      "cache": false,
      "outputs": []
    }
  }
}
