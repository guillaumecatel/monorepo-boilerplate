import { describe, expect, it } from 'vitest'

import {{ camelCase name }} from '@/{{kebabCase name}}'

describe('{{ camelCase name }}', () => {
  it('should say Hello, {{ camelCase name }}!', () => {
    expect({{ camelCase name }}()).toBe('Hello, {{ camelCase name }}!')
  })
})
